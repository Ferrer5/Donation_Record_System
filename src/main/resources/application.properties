# ===============================
# = SPRING PROFILES
# ===============================
spring.profiles.active=@activatedProperties@

# ===============================
# = DATABASE
# ===============================
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/project2?useSSL=false&allowPublicKeyRetrieval=true}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# ===============================
# = SERVER CONFIGURATION
# ===============================
server.port=${PORT:8080}
server.servlet.context-path=/

# ===============================
# = EMAIL CONFIGURATION
# ===============================
spring.mail.host=${SMTP_HOST:smtp-relay.brevo.com}
spring.mail.port=${SMTP_PORT:587}
spring.mail.username=${SMTP_USER:9cf1cf001@smtp-brevo.com}
spring.mail.password=${SMTP_PASSWORD:1J4DAON0ghbGVcqz}
spring.mail.from=${SMTP_FROM:ferrerjanrey001@gmail.com}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
spring.mail.properties.mail.debug=${MAIL_DEBUG:true}

# ===============================
# = ACTUATOR (Production Monitoring)
# ===============================
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=when-authorized
management.endpoint.health.roles=ADMIN